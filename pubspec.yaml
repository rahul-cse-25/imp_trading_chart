name: imp_trading_chart
description: >
  A high-performance trading chart engine for Flutter, inspired by
  TradingView Lightweight Charts. Built as an engine-first, CustomPainter-based
  solution optimized for large datasets, real-time updates, and smooth
  pan/zoom interactions.
version: 0.1.0
homepage: https://github.com/rahul-cse-25/imp_trading_chart
repository: https://github.com/rahul-cse-25/imp_trading_chart
issue_tracker: https://github.com/rahul-cse-25/imp_trading_chart/issues

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
# This package does not bundle assets or fonts.
# It is a pure rendering & widget package.
